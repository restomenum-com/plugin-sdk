{
  "name": "restomenum-sample-plugin",
  "version": "1.2.0",
  "private": true,
  "description": "Restomenum eklenti platformu — resmi @restomenum/plugin-sdk ile örnek/starter eklenti (Connect + webhook imza + senkron aksiyon)",
  "type": "module",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@restomenum/plugin-sdk": "^1.3.0"
  },
  "scripts": {
    "start": "node --env-file=.env src/index.mjs",
    "check": "for f in $(find src test -name '*.mjs'); do node --check \"$f\" || exit 1; done",
    "test:events": "node test/replayEvents.mjs",
    "test:api": "node test/smokeApi.mjs",
    "test:orders": "node test/smokeOrders.mjs"
  }
}
